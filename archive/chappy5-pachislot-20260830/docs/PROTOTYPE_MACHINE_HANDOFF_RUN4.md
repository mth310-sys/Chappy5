# Dual Prototype Machine Handoff — Run 4

Date: 2026-08-29 JST
Role: Machine Director
Delivery target: 2026-08-29 12:00 JST

## Source of truth re-read

Re-read latest `main`, `README.md`, `docs/PACHISLOT_PRODUCTION_WORKFLOW.md`, `docs/REALITY_PRESENTATION_STANDARD.md`, `docs/PROTOTYPE_DUAL_TRACK_STATUS.md`, both integrated prototype paths, latest Playtest & QA evidence, and recent commits before changing code.

Human Gate 2 remains closed. SIGNAL / FORGE / VAULT are unchanged.

## Largest current bottleneck

The latest Generic Autonomous Playtest on commit `2ddb3ac` (run `33215250126`) still produced a WebKit **Target crashed** failure in 刻ノ一閃 during the 30G repeated-play test. The same run passed the generic iPhone interaction smoke and passed ノクターン・アクアリウム's 50G repeated-play test.

Artifact evidence (`9703166451`) records the Toki failure at `toki-round-2`. Before the first recorded round the page had only 77 DOM nodes, 0 active animations, stable 390×664 geometry, and normal control dimensions. This makes accumulated DOM/control drift a poor primary explanation. The remaining submission risk is Toki-specific WebKit rendering/compositor pressure.

## Machine correction applied

Commit `6ad19f3` rewrites `prototypes/toki-no-issen/play.html` as a lean integrated presentation path while preserving the machine's required core loop:

`BET → LEVER → REEL SPIN → STOP1 → STOP2 → STOP3 → sword-action resolution → NEXT BET`

Preserved:
- black / violet / gold sword-machine identity from `index.html`
- three physical reels and fixed operation deck
- STOP1 / STOP2 / STOP3 ordering feedback
- STOP-linked slash and cabinet response
- result-linked audio response
- Safari user-gesture AudioContext start/resume handling
- lightweight reel-node reuse and suppression of the old 78ms reel update path

Removed or flattened in the integrated path to reduce WebKit GPU/compositor risk:
- nested animated `filter` chains
- `mix-blend-mode` compositing on the LCD reflection path
- reel `perspective`
- `will-change` promotion on spinning reels
- brightness/filter button presses in favor of box-shadow only
- the previous injected residue/blade-depth/impact layer stack

This is not a visual redesign toward Nocturne. Toki remains a horizontal sword-action machine; Nocturne remains the vertical aquarium/instrument machine.

## ノクターン・アクアリウム decision

Do not touch the stable Nocturne integrated code in this Machine pass. Its 50G WebKit run is the comparison baseline. Preserve first-STOP observation selection, reel-derived memory interpretation, aquarium depth, optical linkage, and the quiet audio hierarchy.

## Next Game & Reel Director

1. First read the new WebKit run generated by `6ad19f3` (and any later main commit before acting).
2. If Toki 30G passes, keep this lean rendering envelope and improve only the repeated-play information/rhythm inside it; do not restore removed compositor-heavy effects.
3. If Toki still crashes, stop feature expansion. Isolate the remaining base `index.html` CSS/animation path or iframe architecture before adding game content.
4. Keep Nocturne as the stable 50G comparison baseline unless fresh evidence shows a regression.
5. Prototype tuning values remain temporary; do not expand payout/spec complexity before submission.

## Verification state

- ノクターン 50G WebKit: PASS on run `33215250126`.
- 刻ノ一閃 30G WebKit after `6ad19f3`: awaiting fresh CI evidence at handoff time.
- Actual iPhone Safari feel/audio/Bluetooth: HUMAN_UNVERIFIED.
- REALITY_PRESENCE: not Human-verified.
- Human Gate 2: CLOSED.
